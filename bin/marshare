#!/usr/bin/env bash

REMOTEPATH="marand.dk/share/"

# needs marand in ~/.ssh/config
scp -r "$@" marand:/var/www/marand/share

echo ""
for FILENAME in $@
do
    echo "https://$REMOTEPATH$FILENAME"
    (cd ~/bin/windows-stuff && powershell.exe -File QRCodeURI.ps1 https://$REMOTEPATH$FILENAME)
done

